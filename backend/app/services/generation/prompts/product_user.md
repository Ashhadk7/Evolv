Idea: {idea}
Positioning: {positioning}

Primary persona — ground every feature in these pains and jobs:
{persona}

Build the MVP feature specification:
- Group features by module and cover the core user journey, not just the headline feature.
- Every feature must name the persona pain or job it addresses; drop anything that serves none.
- Mark MVP-critical features "Must", strong-v1 "Should", and later ones "Could".

Then return outOfScope items that should wait until after MVP validation, and the build phases.
