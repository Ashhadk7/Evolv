export type {
  PersonType,
  InboxFilter,
  RequestStatus,
  RequestDirection,
  Message,
  Contact,
  InboxLaunchContact,
  StoredProfile,
  StoredAppUser,
} from "./shared-inbox-types";
import type { CurrentInboxUser } from "./shared-inbox-types";

export type CurrentFounder = CurrentInboxUser;
